/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['leadsgen-pro.shop', 'license.leadsgen-pro.shop'],
  },
}

module.exports = nextConfig
